# Flutter Web (Render) Production
# These are not used directly by Flutter web; they are for reference.
# API URL is configured in web/config.js to point to Render backend.

# Backend URL (for reference)
BACKEND_URL=https://backend-sow.onrender.com

# Frontend URL (for reference)
FRONTEND_URL=https://frontend-sow.onrender.com
